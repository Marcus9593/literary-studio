# V2.8 · Phase D 冒烟清单（P1 运营验证）

> **阶段**：运营验证期 — 非开发期  
> **前置**：D1/D2/D3 ✅ · Legacy Guard 已启用  
> **通过标准**：真实使用一轮 + 下列 10 条全部 ✅ → Phase D **COMPLETED** → 再进入 D4 Cleanup B

---

## 架构成熟度（评审参考）

| 维度 | 评估 |
|------|------|
| Domain Boundary | 95%+ |
| Identity Model | 95%+ |
| Measurement Truth | 95%+ |
| Version Domain | 95%+ |
| Migration Safety | 100% |
| Consumer Migration | 95%+ |
| Legacy Removal | ~70%（**故意落后**，D4 再做） |

```text
遗留代码存在 ≠ 架构失败
遗留代码删错   = 生产事故
```

---

## 冒烟项（10 条）

记录方式：每项填 `✅/❌`、日期、操作人、备注。

### Version Domain

| # | 项 | 验证点 | 建议操作 | 结果 |
|---|-----|--------|----------|------|
| 1 | 创建版本 | `workspace` → `versions/{id}/files/` | 创作中心 → 项目版本 → **创建版本**；检查 `data/projects/{id}/versions/` 新目录 | ✅ 2026-06-02 |
| 2 | 查看版本详情 | `metadata.json` + `files/` 可读 | `GET /api/projects/{id}/versions/{versionId}` 或 UI 列表项 | ✅ |
| 3 | 版本 Diff | 旧版 vs 当前 workspace | UI「差异」或 `GET …/versions/{id}/diff` | ✅ |
| 4 | 版本恢复 | 正文与大纲恢复正确 | UI「恢复」后抽查章节 MD | ✅ |

### Knowledge Domain

| # | 项 | 验证点 | 建议操作 | 结果 |
|---|-----|--------|----------|------|
| 5 | 角色解析 | `欧阳逸清` → `char_*` | `node backend-node/story-kb/entity-resolver.spec.mjs` 或 Story OS 读角色 | ✅ |
| 6 | Alias 解析 | `逸清` → 同一 `char_*` | Resolver / 含 alias 的正文场景 | ✅ |
| 7 | Legacy ID | `legacy_id` → `canonicalId` | `migration/id-mapping.json` 与 `resolveByLegacyId` | ✅ |

### Measurement Domain

| # | 项 | 验证点 | 建议操作 | 结果 |
|---|-----|--------|----------|------|
| 8 | Review | 只写 `measurement/review/latest.json` | 运行审稿；确认 **无** `studio.review_by_project` 新增 | ✅ |
| 9 | Verify | `verify/log` 正常；无 `health_snapshot` 复活 | 触发一次 Verify；确认无 `verify/health_snapshot.json` 新建 | ✅ |

### Planning Domain

| # | 项 | 验证点 | 建议操作 | 结果 |
|---|-----|--------|----------|------|
| 10 | Planner 链路 | Planner → Resolver → Knowledge；非 name-as-id | 生成 Story OS Plan；`character_id` 为 `char_*` | ✅ |

---

## 快速命令（可选）

```bash
# 项目 ID 示例
PID=583e5628-24b

# Entity Resolver 集成
node backend-node/story-kb/entity-resolver.spec.mjs

# 迁移仍合法
node backend-node/scripts/migration-validate.mjs --project "$PID"

# Review 后检查 studio 未写入（Guard 应拦截 POST 到旧路径）
# measurement 应有新时间戳
ls -la literary-studio/data/projects/$PID/measurement/review/
```

---

## P1 两段验证

| 段 | 内容 | 文档 |
|----|------|------|
| **第一段** | 自动化（Resolver · validate · Version API · Guard） | [P1-Automation-Verification.md](./P1-Automation-Verification.md) ✅ 2026-06-02 |
| **第二段** | 真实创作闭环（勿为测而测） | [P1-Creative-Validation.md](./P1-Creative-Validation.md) + 下文 10 条 |

### 真实创作闭环（第二段建议路径）

```text
创建版本 → 写章节 → 理解分析 → 建议生成 → Planner 更新
    → 再创建版本 → 版本差异 → 恢复历史版本
```

观察：Identity 全链 `char_*` · Version 四步 · Measurement 增长且无 health_snapshot。

---

## 通过后（四门）

| 门 | 条件 |
|----|------|
| ① | 自动化验证 PASS |
| ② | 冒烟清单 10/10 |
| ③ | 真实创作闭环 ≥1 次 |
| ④ | 连续运行 ≥3 天无新增架构缺陷 |

→ Phase D **COMPLETED** → D4 Cleanup B（仅删 fallback writer，保留 warn reader）

D4 范围（签字后执行）：

```text
删除：studio fallback writer（若仍有）
删除：knowledge root fallback writer（若仍有）
保留：review-facade / store 的 warn-only reader
```

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-02 | P1 清单初稿（CTO 10 条） |
