[pytest]
testpaths = modules scenarios
pythonpath = .
addopts = -v --tb=short --ignore=tests/_backup
filterwarnings =
    ignore::DeprecationWarning

markers =
    integration: 需要运行中的 Literary Studio 后端服务
