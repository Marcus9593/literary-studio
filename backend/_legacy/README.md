# 已归档：Python FastAPI 后端

此目录为 v2.6 之前的 **完整 Python 后端**（`main.py` + `engine.py` + `storage.py` 等），功能已由 `backend-node/` 接管。

- **不要** 在生产或 `npm start` 流程中启动
- 保留仅供对照历史实现；新功能请写在 `backend-node/`
- 相对导入均在本包内（`from .config import …`）
